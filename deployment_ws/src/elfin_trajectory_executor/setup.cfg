# 实机代码 (site / real-cell). Not part of the Gazebo simulation stack.
[develop]
script_dir=$base/lib/elfin_trajectory_executor
[install]
install_scripts=$base/lib/elfin_trajectory_executor

[tool:pytest]
testpaths = test
pythonpath = .
