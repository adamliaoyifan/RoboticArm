# Open cross-agent questions

Read this at session start. If `to` is your role or `any`, append a reply
in the thread, then remove the row (or leave it with a one-line note if
still blocked). Add a row when you need another CLI/role to answer.

| id | to | from | cli | thread | question |
|---|---|---|---|---|---|
