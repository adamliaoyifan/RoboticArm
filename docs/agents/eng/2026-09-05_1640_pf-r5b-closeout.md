# 2026-09-05 16:40 — PF-R5B exact-revision closeout

- role: eng
- agent: claude
- model: glm-5.3
- cli: claude-code
- status: done
- parent: PFH-R5-CLOSURE-20260905
- subtask: PF-R5B
- base_revision: a3dba5e（含 PF-R5A/FIX1/FIX2）
- started_at: 2026-09-05T16:17:26+08:00
- completed_at: 2026-09-05T16:40:00+08:00

## Requirement

docs/plans/platform_free_height_closure.md PF-R5B + reviews 派发
（2026-09-05_1534 线程）：在干净集成 commit 上补齐精确 revision 证据、
双负控、在线真值审计（静态+运行时）、逐档正控冒烟（锚定值+身份）、
stop_sim 残留 0；机械判定 run8 复用边界；调和 PF-A3；Gate 4 拆分文档
标记为 proposal。

## Summary

全部完成。证据目录：
`docs/status/evidence/platform_free_height/2026-09-05_1620_pfr5b-closeout/`
（RESULT.md 汇总 + pfr5b_evidence.json + raw_only_negative_control/ +
rerun_boundary_check.md + teardown 记录）。运行环境为隔离 worktree
`/tmp/pfr5b_clean` @ a3dba5e（dirty=0）。

## 机械复用判定（三项均不变 → 复用 run8）

1. `mesh_observable_reference` 核心计算（band/bin/lid/extent）：0 改动，
   仅新增身份字符串与 fail-path 包装；六锚定值由
   test_pf_r5a_gt_fail_closed 以 places=3 锚定通过。
2. 评测器源码（gate4_eval.py + gate4_scoring.py）：c5921d5..a3dba5e
   **零 diff**。
3. 在线检测路径（detector/filter/segmenter/pipeline/config）：**零 diff**。

## 运行验证（a3dba5e，semantic 栈 + raw 栈两次 launch）

- 参数快照：5 个在线节点全量 `ros2 param dump`
- 静态审计：AST 代码级（排除 docstring）GetCurrentBox/size_eval 引用 = 0
- 运行时审计：detector/filter/waypoint/vacuum/scene_manager 订阅表无
  eval-truth 话题
- 无箱负控：36 帧 DETECT_NO_CLOUD，0 有效
- 三档正控：GT dims == 锚定（±1.5mm）；current_box JSON 携带
  version + stl_sha256 + visual/tier 身份
- raw-only 负控（gate4_eval --negative-control-raw-only, 3 trial）：
  全帧 DETECT_CARGO_SEGMENTATION_REQUIRED、0 有效输出
- 清场：stop_sim 后按进程名计数残留 0（pgrep 模式自匹配的 shell 包装
  不计入；方法记录于 teardown_final.txt）

## 过程缺陷（记录）

- controller_manager 启动竞态复现一次（joint_state_broadcaster spawner
  60s 超时 fatal，伴随相机链死）；整栈重启后恢复。该竞态已多次出现，
  建议列为独立基础设施子任务（launch 侧重试/顺序化）。
- 证据采集器初版两处自身缺陷（docstring 命中静态审计、spawn 后未清箱
  导致"无箱"控有箱）——已修正后重采，非被测系统问题。

## PF-A3 调和

check-8（catalog 回退）在 24060c7→192a6aa→5d677ac 修复；本 closeout
三档正控 + 失败路径测试证明 GetCurrentBox 只携带 STL 可观测参考且失败
零污染零 RNG 副作用。spawner 侧 gt_readiness 阻塞解除。

## Gate 4 拆分文档

`platform_free_height_gate4_revision.md` 头部已标记
**PROPOSAL — pending explicit user confirmation**；PF-R5 关闭不依赖它。

## Verification

- 证据 JSON `overall_pass: true`；负控 gate4_pass true；残留 0
- 负责包回归（R5A/FIX 系列已跑）：description 135 / gazebo 48 /
  perception 461

## Result

- pass: PF-R5B 验收契约在 a3dba5e 补齐；PF-R5 算法结果（run8）+ 验收
  证据（本 closeout）齐备，可作 canonical 关闭依据。

## Pointers

- `docs/status/evidence/platform_free_height/2026-09-05_1620_pfr5b-closeout/RESULT.md`
- `docs/plans/platform_free_height_closure.md`

## Open

- controller_manager 启动竞态（基础设施，独立子任务建议）
- SIM 三箱闭环门 / 代表性检测硬门（closure plan 之外的后续线）

## Closeout-checklist completion (2026-09-05 16:5x supplement)

Re-verified against the plan's Closeout section; three gaps found and
closed:

1. **PF-A3 reconciliation posted to the audit thread** (canonical
   visibility to the test owner): 2026-09-04_2022 thread, Q-20260905-8 —
   check-8 fixed across 24060c7→192a6aa→5d677ac and verified at
   a3dba5e; test owner asked to update gt_readiness on their side.
2. **Canonical PF-R5 thread pointer**: 2026-09-04_1453 thread,
   Q-20260905-9 — states run8 passed the original gate without
   relaxation and points to both evidence sets.
3. **Exact commands/profile recorded**: `COMMANDS.md` added to the
   evidence directory (worktree, build, both launch profiles, collector
   invocation, teardown, boundary-comparison commands).

"User ruling" language: the Gate 4 proposal contains none (header says
"in-session user direction, not yet confirmed"); the only occurrence is
the plan's own requirement text.
