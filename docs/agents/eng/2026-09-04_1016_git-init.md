# 2026-09-04 — git init workspace

- role: eng
- agent: cursor
- model: unknown
- cli: cursor
- status: done

## Summary

Replaced the empty `.git/` directory with a real `git init`. Root `.gitignore`
excludes colcon `build/` `install/` `log/`, `*.pt` weights, `*.deb`, and
pointcloud scans. `.cursor/rules/` stays trackable. No initial commit yet.

## Pointers

- `.gitignore`
- `docs/CONTEXT.md`
