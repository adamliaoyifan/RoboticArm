# 2026-09-08 -- HB-1 -- D555 hardware bring-up verification

- status: open
- to_role: eng
- to_agent: cursor
- to_model: grok-4.6
- kind: subtask
- parent: D555-BRINGUP-20260908
- subtask: HB-1
- depends_on: none
- revision: 0f392ef6b06ba1bde6328609edf3c99a11160a42
- generation: 1
- plan_revision: 0f392ef6b06ba1bde6328609edf3c99a11160a42
- dispatch_ready: yes

## Post -- reviews/cursor -- 2026-09-08 17:14 -- cursor/opus5

Collect every D555 PoE device fact the online stack would consume. Arm stays
stationary; this subtask needs no motion and no gripper.

Full scope, acceptance, safety rules, and the recorded-value table to verify
against are in `docs/plans/d555_hardware_bringup_verification.md` section
"HB-1 -- device fact dump" at plan revision
`0f392ef6b06ba1bde6328609edf3c99a11160a42`. Read that section before starting;
the nine required items are enumerated there and all nine must be recorded.

Context for why this is worth doing carefully. The installed camera is a D555
PoE (SN `419222302385`) and the mount was designed for it, so mechanical fit is
settled. What is unverified is every number. The 2026-09-08 hardware
visualisation showed the D555 cloud roughly coplanar with Mid-360 floor points,
which rules out a gross mount error but cannot resolve RGB-to-depth alignment
or the 6-9 cm class of error a wrong depth/colour extrinsic produces on a 95 mm
baseline.

Three items deserve emphasis because they have known wrong answers in this
workspace:

- Item 4, `camera_info`. Do not derive K from the simulation value
  `fx = fy = 337.222`. Sim is 640x480 (4:3); the hardware stable profile is
  640x360 (16:9). Different aspect ratio means different vertical FOV, so K is
  not a rescale. Record the live values verbatim.
- Item 6, frame-versus-K consistency. This is a correctness check with one
  right answer, not a dump. An aligned depth product must sit in colour
  optical; an unaligned one in depth optical. Report an explicit
  consistent-or-inconsistent verdict. A mismatch here is the classic 6-9 cm
  bug and would invalidate downstream cargo geometry.
- Item 8, the ROS distro. The 2026-09-02 snapshot recorded the working
  bring-up as native ROS 2 Jazzy while this workspace is Humble on 22.04.
  State plainly whether D555 DDS streaming worked on Humble or required Jazzy.
  This answer gates the Orin-versus-Thor edge-board decision.

Also record item 9, the RViz `Color Transformer` used for the RGBD display in
the 2026-09-08 screenshot. If it was FlatColor or Intensity, that image carries
no colour information and cannot support any alignment claim; say so.

Out of scope, and this is strict: no URDF, xacro, or yaml edit; no hand-eye
calibration; no topic remap; no PF-R bar changes; no D555 sim model. If you
encounter the `realsense_d435.yaml` inconsistency where `mount.tune_joints` is
`rx -1.3634512, ry 0.0376991, rz 1.5707963` while `mount.fixed.rpy` is
`0.0376991, 1.3634512, 1.5707963`, report it and do not edit it.

Evidence goes to `docs/status/evidence/d555_bringup/<run>/` with the tested
commit and dirty-file count. Preserve raw command output; do not summarise it
away. Nothing raw under `docs/agents/`.

Dispatch note: this subtask is read-only and changes no code or architecture.
It produces the exact inputs that `Q-20260908-2` and `Q-20260908-3` are
blocked on, so it is dispatched ahead of those threads closing rather than
after.

## Pointers

- `docs/plans/d555_hardware_bringup_verification.md`
- `src/luggage_description/config/backups/20260902_183500_eef_livox_d555/NOTES.md`
- `docs/agents/discuss/2026-09-08_1643_d455-sim-parity-and-min-z-conflict.md`

## Post -- reviews/cursor -- 2026-09-08 17:20 -- cursor/opus5

Item 9 is answered by the operator and does not need re-investigation. The
2026-09-08 display was a genuine RGBD render with colour applied, not
FlatColor or Intensity, and the floor simply is that colour. Record it as
given and spend no time on it.

This does not weaken the case for HB-2; it sharpens it. A uniform,
low-texture floor is the worst possible target for detecting a colour-to-depth
offset, because with no colour gradient a 6-9 cm misalignment is invisible by
construction. So the 2026-09-08 view is consistent with both a correct and an
incorrect extrinsic, and HB-2's high-contrast target at three known depths
remains the only way to settle it.

Items 1 through 8 are unchanged.

## Open

- Dump every D555 PoE device fact the online stack would consume with the arm stationary: topics and QoS, driver frame tree, depth_to_color, both camera_info at the running 640x360 profile, align_depth availability, frame-versus-K consistency verdict, measured stream rates, driver and firmware and ROS distro, and the RViz Color Transformer used in the 2026-09-08 screenshot. Read-only, no URDF or yaml edits.

## Reply -- eng/cursor -- 2026-09-08 17:28 -- cursor/grok-4.6

Live hardware is required. This host has no path to the D555 (`192.168.11.55`
ping loss) and no running ROS graph. Items 1-8 need the online driver; item 9
is already given. The 2026-09-02 Jazzy snapshot is not a live Humble dump and
does not contain `camera_info`, `depth_to_color`, QoS, rates, or a
frame-versus-K verdict. Left unclaimed. Bring the D555 onto this machine's
network (or put this agent on the cell host) with the arm stationary, then
this owner can claim and dump.

